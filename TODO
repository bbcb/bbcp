Выяленные проблемы:

И.Денисов
* вылетает при попытке открытия файлов (Ubuntu);
* отображение скроллеров;
* главная петля.

А.Ширяев
* размер главного окна (не Ubuntu)
* утечка памяти в HostFonts или в HostPorts или ещё где;
* проблема с DevDebug и Kernel.GetRefVar;
* верхняя (первая) строчка документа отображается наполовину, если используется большой шрифт;
* проблема с выделением в документах;
* курсор в текстовых документах отображается не всегда;
* если в меню используется кириллица, то отображается неправильно (нужно преобразовывать кодировку в UTF-8 в HostMenus);
* HostPackedFiles: убрать зависимость HostFiles.appName, если надо, то сделать через BB_APPNAME; сравнить с Windows-версией (BlackBox 1.6-rc6);
* обновить документацию Lin/Docu/LinuxPort: /USE -> BB_PRIMARY_DIR, BB_SECONDARY_DIR; HostFiles.appName не используем.
